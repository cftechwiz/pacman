<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= message %></title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <h1><%= message %></h1>
    <% if (error && error.status) { %>
      <h2><%= error.status %></h2>
    <% } %>
    <% if (error && error.stack) { %>
      <pre><%= error.stack %></pre>
    <% } %>
  </body>
</html>
